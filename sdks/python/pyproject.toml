[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "nanolambda"
version = "0.1.0"
description = "Python SDK for NanoLambda AI-agent code-execution sandbox"
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.10"
dependencies = ["httpx>=0.25"]

[project.urls]
Homepage = "https://nanolambda.io"
Repository = "https://github.com/ip888/nanolambda"
